const $ = (selector) => document.querySelector(selector);

const aChoice = $('#a-radio');

const bChoice = $('b-radio');

const cChoice = $('#c-radio');

const dChoice = $('#d-radio');

document.addEventListener('DOMContentLoaded', () => {

});
